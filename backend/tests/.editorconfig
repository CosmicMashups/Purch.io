[*.cs]
# Underscore-separated test names (Given_When_Then style) are the standard xUnit
# convention and clearer than the alternative here — this is a deliberate override,
# not a lapse in the "robust backend" analyzer discipline applied to src/.
dotnet_diagnostic.CA1707.severity = none
