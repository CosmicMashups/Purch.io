# Production/preview builds call the API directly, so its CORS_ALLOWED_ORIGINS must include this site's origin.
VITE_API_BASE_URL=https://purch-io-backend.vercel.app

# Dev only (unset in dev, which defaults to the /api proxy): where the Vite proxy forwards /api.
# VITE_DEV_PROXY_TARGET=https://localhost:5001
