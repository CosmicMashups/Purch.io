# Local/On-Prem Installer

Packaging for the Local deployment mode (see `docs/ARCHITECTURE.md` and the implementation plan): bundled Postgres setup, self-contained backend executable/Windows service, deployment-mode config template, LAN discovery/manual-IP flow for POS devices.

Built out in Phase 10, once the backend exists. Empty scaffold for now.
